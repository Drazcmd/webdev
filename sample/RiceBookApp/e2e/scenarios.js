'use strict'

describe('RiceBookApp', function() {

	if('should have user message', function() {
		browser.get('index.html')
		// expect(
		// 	element.all(by.css('h2')).first().getText()
		// 	).toMatch('Foo')
		expect(1).toBe(1)
	})
});
